// doubly linked list 
#include <bits/stdc++.h>
using namespace std;

typedef struct node {
    int data;
    struct node *next;
    struct node *prev;
} Node;

Node *create_list(int data) {
    Node *head = NULL, *temp = NULL, *current = NULL;
}
int main() {
    
    return 0;
}