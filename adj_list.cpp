// adjacency linked list graph
#include <bits/stdc++.h>

using namespace std;

typedef struct edge {
    int edge;
    struct edge *next;
} Edge;


typedef struct graph {
    
} Graph;

int main() {
    
    return 0;
}
