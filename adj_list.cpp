// adjacency linked list graph
#include <bits/stdc++.h>

using namespace std;

typedef struct edge {
    int edge;
    struct edge *next;
} Edge;


typedef struct graph {
    
} Graph;

Graph *createGraph(int V);

int main() {
    
    return 0;
}
