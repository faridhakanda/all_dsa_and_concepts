// breadth first search
// graph data structure
#include <bits/stdc++.h>
using namespace std;


typedef struct edge {
    int data;
    struct edge *next;
} Edge;

typedef struct graph {
    int vertex;
    Edge *edge;
} Graph;
int main() {

    return 0;
}
