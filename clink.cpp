// circular linked list
#include <bits/stdc++.h>
using namespace std;

typedef struct node {
    int data;
    struct node *next;
} Node;

Node *create_list(int data);
void print_list(Node *head);
int main() {

    return 0;
}